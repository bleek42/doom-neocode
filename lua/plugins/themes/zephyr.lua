return {

    {
        "glepnir/zephyr-nvim",
        name = "zephyr",
        lazy = true,
        event = 'VeryLazy',

        -- setup = function()

        -- end,

        -- opts = {},
        -- config = function(_, opts)
        --     require('zephyr')
        --     -- vim.cmd('colorscheme zephyr')
        -- end,
    }

}
