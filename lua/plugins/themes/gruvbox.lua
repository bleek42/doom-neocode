return {
    {
        "ellisonleao/gruvbox.nvim",
        name = "gruvbox",
        lazy = true,
        event = 'VeryLazy',
        -- setup = function()

        -- end,

        -- opts = function()
        --     -- vim.o.background = "dark"
        -- end,

        -- config = function(_, opts)
        --     vim.cmd("colorscheme gruvbox")
        -- end
    }
}
